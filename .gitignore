# 1. すべてのファイルを一旦無視
*

# 2. ディレクトリ構造は維持する
!*/

# 3. 管理に含めたいファイルだけを「除外対象から外す（!）」
!*.c
!*.h
!*.txt
!Makefile
!.gitignore
!*.md
!*.sh

# 4. 個別に除外したいVim関連や一時ファイル
#（上記で許可したもの以外はすでに無視されていますが、念のため）
*.swp
*.swo
*~
tags
